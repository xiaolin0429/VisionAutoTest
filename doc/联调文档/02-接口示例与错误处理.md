# 接口示例与错误处理

## 1. 后端初始化示例

### 1.1 本地开发初始化

```bash
cd backend
source .venv/bin/activate
python -m app.db.bootstrap
```

预期效果：

- 若 `AutoTestDev` 不存在则自动创建
- 自动执行 Alembic 迁移
- 自动写入默认管理员

### 1.2 启动后端

```bash
uvicorn app.main:app --reload
```

### 1.3 健康检查

```http
GET /healthz
```

成功示例：

```json
{
  "data": {
    "status": "ok",
    "service": "VisionAutoTest Backend"
  },
  "meta": {
    "request_id": "req_xxx"
  }
}
```

## 2. 登录

### 2.1 请求

```http
POST /api/v1/sessions
Content-Type: application/json

{
  "username": "admin",
  "password": "admin123456"
}
```

### 2.2 响应

```json
{
  "data": {
    "session_id": "ses_xxx",
    "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxx.yyy",
    "refresh_token": "rfr_xxx",
    "token_type": "Bearer",
    "expires_in": 7200,
    "user": {
      "id": 1,
      "username": "admin",
      "display_name": "Administrator"
    }
  },
  "meta": {
    "request_id": "req_xxx"
  }
}
```

说明：

- 上述 `password` 仅为示例值，实际联调请以本地 `backend/.env` 中 `VAT_DEFAULT_ADMIN_PASSWORD` 为准
- `access_token` 已升级为 JWT，前端仍继续使用 `Authorization: Bearer <token>`

## 3. 上传媒体并创建模板

### 3.1 上传媒体对象

```http
POST /api/v1/media-objects
Authorization: Bearer <token>
X-Workspace-Id: 1
Content-Type: multipart/form-data
```

表单字段：

- `file`: 图片文件
- `usage`: `baseline`
- `remark`: 可选

### 3.2 创建模板

```http
POST /api/v1/templates
Authorization: Bearer <token>
X-Workspace-Id: 1
Content-Type: application/json

{
  "template_code": "tpl_login_page",
  "template_name": "登录页模板",
  "template_type": "page",
  "match_strategy": "template",
  "threshold_value": 0.95,
  "status": "published",
  "original_media_object_id": 5001
}
```

说明：

- 创建模板时会自动生成首个基准版本
- 当前 `match_strategy` 仅支持 `template` 与 `ocr`
- 当前模板状态建议使用 `draft / published / archived`，其中仅 `published` 模板可用于执行
- `template_assert` 要求模板 `match_strategy=template`
- `ocr_assert` 若关联模板，则要求模板 `match_strategy=ocr`

## 4. 写入用例步骤示例

### 4.1 普通步骤 + 组件调用

```http
PUT /api/v1/test-cases/2001/steps
Authorization: Bearer <token>
X-Workspace-Id: 1
Content-Type: application/json

[
  {
    "step_no": 1,
    "step_type": "wait",
    "step_name": "页面预热",
    "payload_json": {
      "ms": 200
    }
  },
  {
    "step_no": 2,
    "step_type": "component_call",
    "step_name": "调用共享登录组件",
    "component_id": 1001
  },
  {
    "step_no": 3,
    "step_type": "input",
    "step_name": "输入用户名",
    "payload_json": {
      "selector": "#username",
      "text": "admin"
    }
  }
]
```

### 4.2 模板断言步骤

```http
PUT /api/v1/test-cases/2001/steps
Authorization: Bearer <token>
X-Workspace-Id: 1
Content-Type: application/json

[
  {
    "step_no": 1,
    "step_type": "template_assert",
    "step_name": "校验登录页主视觉",
    "template_id": 6001,
    "payload_json": {
      "threshold": 0.97
    }
  }
]
```

### 4.3 OCR 断言步骤

```http
PUT /api/v1/test-cases/2001/steps
Authorization: Bearer <token>
X-Workspace-Id: 1
Content-Type: application/json

[
  {
    "step_no": 1,
    "step_type": "ocr_assert",
    "step_name": "校验页面文案",
    "payload_json": {
      "selector": "#page-title",
      "expected_text": "登录",
      "match_mode": "contains",
      "case_sensitive": false
    }
  }
]
```

## 5. 创建执行批次

```http
POST /api/v1/test-runs
Authorization: Bearer <token>
X-Workspace-Id: 1
Idempotency-Key: run-001
Content-Type: application/json

{
  "test_suite_id": 3001,
  "environment_profile_id": 4001,
  "trigger_source": "manual"
}
```

成功响应要点：

- 初始 `status` 为 `queued`
- 后续通过 `GET /api/v1/test-runs/{id}` 轮询
- 断言失败最终可能返回 `failed` 或 `partial_failed`
- 运行时异常最终可能返回 `error`

## 6. 查询步骤结果

```http
GET /api/v1/case-runs/6001/step-results
Authorization: Bearer <token>
X-Workspace-Id: 1
```

### 6.1 `template_assert` 成功示例

```json
{
  "data": [
    {
      "id": 9101,
      "case_run_id": 6001,
      "step_no": 1,
      "step_type": "template_assert",
      "status": "passed",
      "score_value": 0.9812,
      "expected_media_object_id": 5001,
      "actual_media_object_id": 7001,
      "diff_media_object_id": null,
      "error_message": null
    }
  ],
  "meta": {
    "request_id": "req_xxx"
  }
}
```

### 6.2 `template_assert` 失败示例

```json
{
  "data": [
    {
      "id": 9102,
      "case_run_id": 6002,
      "step_no": 1,
      "step_type": "template_assert",
      "status": "failed",
      "score_value": 0.2134,
      "expected_media_object_id": 5001,
      "actual_media_object_id": 7002,
      "diff_media_object_id": 7003,
      "error_message": "Template assertion failed: score 0.2134 is below threshold 0.9500."
    }
  ],
  "meta": {
    "request_id": "req_xxx"
  }
}
```

### 6.3 `ocr_assert` 失败示例

```json
{
  "data": [
    {
      "id": 9103,
      "case_run_id": 6003,
      "step_no": 2,
      "step_type": "ocr_assert",
      "status": "failed",
      "score_value": 0.0,
      "expected_media_object_id": null,
      "actual_media_object_id": 7004,
      "diff_media_object_id": null,
      "error_message": "OCR assertion failed: expected `登录` but got `注册`."
    }
  ],
  "meta": {
    "request_id": "req_xxx"
  }
}
```

## 7. 查询报告与证据

### 7.1 查询报告详情

```http
GET /api/v1/reports/8001
Authorization: Bearer <token>
X-Workspace-Id: 1
```

成功示例：

```json
{
  "data": {
    "id": 8001,
    "test_run_id": 3001,
    "summary_status": "failed",
    "summary_json": {
      "status": "failed",
      "counts": {
        "total": 1,
        "passed": 0,
        "failed": 1,
        "error": 0,
        "cancelled": 0
      },
      "failure": {
        "code": "TEMPLATE_ASSERTION_FAILED",
        "summary": "Template assertion failed: score 0.2134 is below threshold 0.9500."
      },
      "timing": {
        "started_at": "2026-03-30T10:00:00+00:00",
        "finished_at": "2026-03-30T10:00:03+00:00",
        "duration_ms": 3000
      },
      "artifacts": {
        "total": 2,
        "by_type": {
          "step_actual": 1,
          "step_diff": 1
        }
      },
      "total_case_count": 1,
      "passed_case_count": 0,
      "failed_case_count": 1,
      "error_case_count": 0,
      "cancelled_case_count": 0,
      "message": "Template assertion failed: score 0.2134 is below threshold 0.9500."
    }
  },
  "meta": {
    "request_id": "req_xxx"
  }
}
```

### 7.2 查询报告产物

```http
GET /api/v1/reports/8001/artifacts
Authorization: Bearer <token>
X-Workspace-Id: 1
```

成功示例：

```json
{
  "data": [
    {
      "id": 1,
      "report_id": 8001,
      "artifact_type": "step_actual",
      "media_object_id": 7002,
      "case_run_id": 6002,
      "step_result_id": 9102,
      "artifact_url": "1/obj_xxx_case-run-6002-step-1-actual.png"
    },
    {
      "id": 2,
      "report_id": 8001,
      "artifact_type": "step_diff",
      "media_object_id": 7003,
      "case_run_id": 6002,
      "step_result_id": 9102,
      "artifact_url": "1/obj_xxx_case-run-6002-step-1-diff.png"
    }
  ],
  "meta": {
    "request_id": "req_xxx"
  }
}
```

## 8. 常见错误与排查

### 8.1 后端无法启动

常见原因：

- PostgreSQL 未启动
- `AutoTestDev` 尚未创建
- Alembic 迁移未执行
- 缺少视觉依赖但误以为会影响全部接口

建议排查顺序：

1. 确认 PostgreSQL 可连接
2. 执行 `python -m app.db.bootstrap`
3. 确认后端虚拟环境已安装项目依赖
4. 再次启动 `uvicorn app.main:app --reload`

### 8.2 登录失败

若默认管理员登录失败，优先排查：

- 初始化命令是否执行成功
- 当前连接的是否为 `AutoTestDev`
- 开发库是否被重建但未重新种子管理员

### 8.3 步骤保存失败

#### 缺少模板断言模板 ID

```json
{
  "error": {
    "code": "STEP_SEQUENCE_INVALID",
    "message": "template_assert step requires template_id.",
    "details": []
  },
  "meta": {
    "request_id": "req_xxx"
  }
}
```

当前实际错误码已统一为：

```json
{
  "error": {
    "code": "STEP_CONFIGURATION_INVALID",
    "message": "template_assert step requires template_id.",
    "details": []
  },
  "meta": {
    "request_id": "req_xxx"
  }
}
```

#### OCR 断言缺少必填载荷

```json
{
  "error": {
    "code": "STEP_SEQUENCE_INVALID",
    "message": "ocr_assert step requires payload_json.expected_text.",
    "details": []
  },
  "meta": {
    "request_id": "req_xxx"
  }
}
```

当前实际错误码已统一为：

```json
{
  "error": {
    "code": "STEP_CONFIGURATION_INVALID",
    "message": "ocr_assert step requires payload_json.expected_text.",
    "details": []
  },
  "meta": {
    "request_id": "req_xxx"
  }
}
```

### 8.4 执行批次创建失败

#### 套件未激活

```json
{
  "error": {
    "code": "TEST_SUITE_NOT_ACTIVE",
    "message": "Test suite must be active before execution.",
    "details": []
  },
  "meta": {
    "request_id": "req_xxx"
  }
}
```

#### 存在未发布用例或组件

```json
{
  "error": {
    "code": "PUBLISHED_VERSION_REQUIRED",
    "message": "Component must be published before execution.",
    "details": []
  },
  "meta": {
    "request_id": "req_xxx"
  }
}
```

#### 模板策略与步骤不兼容

```json
{
  "error": {
    "code": "STEP_CONFIGURATION_INVALID",
    "message": "template_assert step requires template match_strategy `template`.",
    "details": []
  },
  "meta": {
    "request_id": "req_xxx"
  }
}
```

#### 模板缺少当前基准版本

```json
{
  "error": {
    "code": "BASELINE_REVISION_REQUIRED",
    "message": "Template must have a current baseline revision before execution.",
    "details": []
  },
  "meta": {
    "request_id": "req_xxx"
  }
}
```

## 9. 联调口径说明

- 前端本地联调默认只关注开发库 `AutoTestDev`
- 测试数据库属于后端测试阶段独立使用，不纳入前端联调流程
- 当前后端已支持 `template_assert / ocr_assert`
- 当前后端不支持 `orb / mixed` 作为可交付模板策略
- 前端提交联调时，重点适配：
  - 登录响应新增 `token_type`
  - 模板策略与模板状态的受限枚举
  - `failed / error` 状态语义
  - 报告摘要结构化字段
  - `report-artifacts` 的 `artifact_type / case_run_id / step_result_id`
