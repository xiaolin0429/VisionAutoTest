# 执行与报告模型

## 1. 表清单

| 表名 | 说明 |
|---|---|
| `exec_test_runs` | 执行批次表 |
| `exec_test_case_runs` | 用例执行实例表 |
| `exec_step_results` | 步骤结果表 |
| `report_run_reports` | 报告主表 |
| `report_artifacts` | 报告证据链表 |

## 2. 表设计

### 2.1 exec_test_runs

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK | 主键 |
| `workspace_id` | BIGINT | NOT NULL, FK | 工作空间 ID |
| `test_suite_id` | BIGINT | NOT NULL, FK | 套件 ID |
| `environment_profile_id` | BIGINT | NOT NULL, FK | 环境档案 ID |
| `device_profile_id` | BIGINT | NULL, FK | 设备预设 ID |
| `trigger_source` | VARCHAR(32) | NOT NULL | `manual/ci/schedule/webhook` |
| `triggered_by` | BIGINT | NULL, FK | 触发人 |
| `idempotency_key` | VARCHAR(128) | NULL | 幂等键 |
| `status` | VARCHAR(32) | NOT NULL | 执行状态 |
| `started_at` | TIMESTAMP | NULL | 开始时间 |
| `finished_at` | TIMESTAMP | NULL | 结束时间 |
| `total_case_count` | INTEGER | NOT NULL DEFAULT 0 | 总用例数 |
| `passed_case_count` | INTEGER | NOT NULL DEFAULT 0 | 通过数 |
| `failed_case_count` | INTEGER | NOT NULL DEFAULT 0 | 失败数 |
| `error_case_count` | INTEGER | NOT NULL DEFAULT 0 | 异常数 |
| `created_at` | TIMESTAMP | NOT NULL | 创建时间 |
| `updated_at` | TIMESTAMP | NOT NULL | 更新时间 |

索引建议：

- `uk_exec_test_runs_idempotency(workspace_id, idempotency_key)`
- `idx_exec_test_runs_status_created(status, created_at)`

### 2.2 exec_test_case_runs

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK | 主键 |
| `test_run_id` | BIGINT | NOT NULL, FK | 执行批次 ID |
| `test_case_id` | BIGINT | NOT NULL, FK | 用例 ID |
| `sort_order` | INTEGER | NOT NULL | 执行顺序 |
| `status` | VARCHAR(32) | NOT NULL | 用例执行状态 |
| `started_at` | TIMESTAMP | NULL | 开始时间 |
| `finished_at` | TIMESTAMP | NULL | 结束时间 |
| `duration_ms` | BIGINT | NULL | 耗时 |
| `failure_reason_code` | VARCHAR(64) | NULL | 失败原因码 |
| `failure_summary` | VARCHAR(512) | NULL | 失败摘要 |
| `created_at` | TIMESTAMP | NOT NULL | 创建时间 |

索引建议：

- `idx_exec_test_case_runs_run_status(test_run_id, status)`

### 2.3 exec_step_results

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK | 主键 |
| `case_run_id` | BIGINT | NOT NULL, FK | 用例执行实例 ID |
| `step_no` | INTEGER | NOT NULL | 步骤序号 |
| `step_type` | VARCHAR(32) | NOT NULL | 步骤类型 |
| `status` | VARCHAR(32) | NOT NULL | 步骤状态 |
| `score_value` | DECIMAL(6,4) | NULL | 相似度或评分 |
| `expected_media_object_id` | BIGINT | NULL, FK | 预期图 |
| `actual_media_object_id` | BIGINT | NULL, FK | 实际图 |
| `diff_media_object_id` | BIGINT | NULL, FK | 差异图 |
| `error_message` | VARCHAR(1024) | NULL | 错误信息 |
| `started_at` | TIMESTAMP | NULL | 开始时间 |
| `finished_at` | TIMESTAMP | NULL | 结束时间 |
| `duration_ms` | BIGINT | NULL | 耗时 |
| `created_at` | TIMESTAMP | NOT NULL | 创建时间 |

唯一约束：

- `uk_exec_step_results(case_run_id, step_no)`

### 2.4 report_run_reports

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK | 主键 |
| `test_run_id` | BIGINT | NOT NULL, FK, UK | 执行批次 ID |
| `summary_status` | VARCHAR(32) | NOT NULL | 总体状态 |
| `summary_json` | JSON | NOT NULL | 统计摘要 |
| `generated_at` | TIMESTAMP | NOT NULL | 生成时间 |
| `created_at` | TIMESTAMP | NOT NULL | 创建时间 |

### 2.5 report_artifacts

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | BIGINT | PK | 主键 |
| `report_id` | BIGINT | NOT NULL, FK | 报告 ID |
| `artifact_type` | VARCHAR(32) | NOT NULL | `screenshot/diff/heatmap/video/log` |
| `media_object_id` | BIGINT | NULL, FK | 媒体对象 ID |
| `artifact_url` | VARCHAR(512) | NULL | 对外访问 URL |
| `created_at` | TIMESTAMP | NOT NULL | 创建时间 |

## 3. 设计说明

- 执行与报告为典型流水域，不做逻辑删除。
- `exec_step_results` 存储真实执行后的线性步骤序列；若用例步骤中引用 `component_call`，组件步骤会在运行时展开后再落库。
- 创建执行批次前，应用层需先校验：套件为 `active`，套件内测试用例与引用组件为 `published`。
- 报告主表只存聚合信息，大文件通过 `report_artifacts` 关联。
- 评分、耗时、失败码等字段拆开存储，便于统计分析。
